# Processed Data

This folder contains pre-processed data generated from the `/Data/Raw/` folders.
The data here should be what is referenced in the `/Results/` folders, instead of the raw data itself.
